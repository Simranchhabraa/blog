
<%= form_with(model: idproof) do |form| %>
  <% if idproof.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(idproof.errors.count, "error") %> prohibited this idproof from being saved:</h2>

      <ul>
        <% idproof.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div>
    <%= form.label :document, style: "display: block" %>
    <%= form.file_field :document %>
  </div>

  <div>
  <%= form.label :status %><br>
  <%= form.select :status, Idproof.statuses.keys %>
</div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
